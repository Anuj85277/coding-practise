
// write a program to print numbers between a given range.

#include <stdio.h>

int main() {
	int i,n,m=0;
	scanf("%d",&m);
	scanf("%d",&n);
	for(i=m;i<=n;++i)
	{
	    printf("%d ",i);
	    
	}
	return 0;
}
